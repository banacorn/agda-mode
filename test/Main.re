open BsMocha;
open BsMocha.Mocha;

describe("Array", () =>
  describe("#indexOf()", () =>
    it("should return -1 when the value is not present", () =>
      Assert.equal(-1, -1)
    )
  )
);

describe("Array", () =>
  describe("#indexOf()", () =>
    it("should return -1 when the value is not present", () =>
      Assert.equal(-1, 1)
    )
  )
);
